<?xml version="1.0" encoding="utf-8"?>
<!--
  Only the one directory the camera writes into is shared. Everything else,
  including the saved sketches themselves, stays private to the app.
-->
<paths>
    <cache-path
        name="capture"
        path="capture/" />
</paths>
